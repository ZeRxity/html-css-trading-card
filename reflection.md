The hardest thing for me in this assignment with CSS is the entire thing itself! I've never used nor bother to learn anything about CSS so it was difficult to actual know the syntax and use of all the commands/methods. But, if I were to pick a specific property about CSS, it would any time I had to anything regarding sizes like use px or percentages, since I would always get it wrong the first few tries until I got to my desired size. I know understand the whole use of CSS when it comes to merging it with HTML. If i had more time I'd like to decorate the card like adding a gradient or strips or lines, just like the example Pokémon card. I'd also like to fix some of the font. choices.

